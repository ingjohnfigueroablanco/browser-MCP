{
  "_comment": "Use this when running the MCP server locally (stdio mode, no Docker)",
  "mcpServers": {
    "agenteqa-navigator": {
      "command": "python",
      "args": ["-m", "agenteqa_navigator"],
      "env": {
        "PYTHONPATH": "/absolute/path/to/browser-MCP/src"
      }
    }
  }
}
