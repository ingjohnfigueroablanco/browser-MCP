# Copy to .env and fill in values
# cp .env.example .env

# API key to protect the server (leave empty for open/dev mode)
MCP_API_KEY=

# Port to expose (default 8000)
MCP_PORT=3067

# For ngrok tunnel (optional — exposes your localhost to the cloud browser)
# NGROK_AUTHTOKEN=
